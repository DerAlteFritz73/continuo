{# Silent when no source could answer — better than printing a made-up date. #}
{% if deploy.deployedAt %}
<p class="deploy-stamp">
    {{ 'footer.deployed'|trans }}
    <time datetime="{{ deploy.deployedAt|date('c') }}">{{ deploy.deployedAt|date('Y-m-d H:i', 'UTC') }} UTC</time>
</p>
{% endif %}
